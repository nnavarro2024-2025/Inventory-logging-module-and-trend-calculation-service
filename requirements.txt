pandas>=1.5


# (only if you haven't created the venv)
python -m venv .venv

# activate the venv (PowerShell)
.venv\Scripts\Activate.ps1

# install dependencies
pip install -r requirements.txt

# run the demo
python python/demo.py